import { ProductListFilterPipe } from './product-list-filter.pipe';

describe('ProductListFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductListFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
